server {
  listen 8080;
  server_name _;

  root   /usr/share/nginx/html;
  index  index.html;
  # Content Security Policy tailored for Likelee UI
  # - Keep defaults strict
  # - Remove AWS Rekognition streaming + Cognito (liveness removed)
  # - Allow Supabase Auth endpoint
  # - Allow GTM/GA and inline scripts used by the app bootstrap
  # - Allow Google Fonts
  # - Allow remote https images as needed by the app
  add_header Content-Security-Policy "default-src 'self'; connect-src 'self' https://*.supabase.co https://www.google-analytics.com https://www.googletagmanager.com https://fonts.googleapis.com https://*.veriff.com https://*.veriff.me https://*.probity.io https://cdn.docuseal.com https://docuseal.com https://*.docuseal.com https://*.docuseal.co https://api.docuseal.co; img-src 'self' data: blob: https: https://*.probity.io; media-src 'self' data: blob: https:; script-src 'self' https://www.googletagmanager.com https://www.google-analytics.com https://*.veriff.com https://*.veriff.me https://*.probity.io https://cdn.docuseal.com https://*.docuseal.com https://*.docuseal.co 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://*.veriff.com https://*.veriff.me; font-src 'self' https://fonts.gstatic.com data:; frame-src 'self' https://*.veriff.com https://*.veriff.me https://cdn.docuseal.com https://docuseal.com https://*.docuseal.com https://*.docuseal.co; frame-ancestors 'self';" always;
  add_header Permissions-Policy "camera=(self https://*.veriff.com https://*.veriff.me), microphone=(self https://*.veriff.com https://*.veriff.me)" always;

  # Cache static assets aggressively; index.html no-cache
  location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|ico|webp|woff2?)$ {
    expires 30d;
    add_header Cache-Control "public, max-age=2592000";
    try_files $uri =404;
  }

  location = /favicon.ico {
    return 302 /favicon.svg;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
