# Frontend build-time variables (passed as build args to likelee-ui)
# Fill these and copy to deploy/ec2/.env before building on EC2.

# Keycloak
VITE_KEYCLOAK_URL=https://your-keycloak.example.com/auth
VITE_KEYCLOAK_REALM=likelee
VITE_KEYCLOAK_CLIENT_ID=likelee-web

# Supabase (public anon)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# API base URL the frontend uses. When running behind the gateway, set to /api
VITE_API_BASE_URL=/api

# AWS Liveness (optional; must match backend region if used)
VITE_AWS_REGION=eu-central-1
VITE_COGNITO_IDENTITY_POOL_ID=eu-central-1:xxxxxxxx-xxxx-xxxx-xxxxxxxxxxxx
